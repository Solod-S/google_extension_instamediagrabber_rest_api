export declare class videoUrlResponseDto {
    success: boolean;
    message: string;
    url: string;
    thumbnail: string;
    constructor({ success, message, url, thumbnail }: videoUrlResponseDto);
}
